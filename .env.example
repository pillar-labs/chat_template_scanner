# Optional Pillar API key used when enabling remote template scanning
PILLAR_API_KEY=your-pillar-api-key

# Hugging Face token for accessing private repos (optional)
HUGGINGFACE_TOKEN=your-huggingface-token

# Default request timeout in seconds for remote fetches (optional override)
GGUF_SCANNER_TIMEOUT=30

# Optional overrides for remote header fetch sizing
GGUF_SCANNER_INITIAL_REQUEST_SIZE=2097152
GGUF_SCANNER_MAX_REQUEST_SIZE=8589934592
